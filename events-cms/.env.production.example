# Production env for events-cms
# Copy to .env.production before running: npm run build
# Create React App loads .env.production automatically during build.

# Backend API – use your live backend URL
REACT_APP_API_URL=https://app.evential.sg:5000
REACT_APP_API_BASE_URL=https://app.evential.sg:5000

REACT_APP_API_NODE_ENV=production

REACT_APP_API_TIMEOUT=15000
REACT_APP_API_RETRY_ATTEMPTS=5
REACT_APP_API_RETRY_DELAY=2000
REACT_APP_ENABLE_LOGGING=false

# Optional: smaller build (no source maps)
# GENERATE_SOURCEMAP=false
